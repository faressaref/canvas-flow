import fs from "node:fs";

const file = "public/index.html";
let s = fs.readFileSync(file, "utf8");

const injected = `<script id="canvasflow-image-persistence-fix">(function(){const TOTAL_BUDGET=760*1024;function isDataImage(u){return typeof u==="string"&&u.indexOf("data:image/")===0}function isHttp(u){return typeof u==="string"&&/^https?:\\/\\//i.test(u)}function loadImage(u){return new Promise((resolve,reject)=>{const im=new Image();im.onload=()=>resolve(im);im.onerror=()=>reject(new Error("تعذر تجهيز الصورة للحفظ"));im.src=u})}function blobToDataUrl(b){return new Promise((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(r.result);r.onerror=reject;r.readAsDataURL(b)})}async function compressImage(u,maxSide,q){try{const im=await loadImage(u),ow=im.naturalWidth||im.width||1,oh=im.naturalHeight||im.height||1,scale=Math.min(1,maxSide/Math.max(ow,oh)),nw=Math.max(1,Math.round(ow*scale)),nh=Math.max(1,Math.round(oh*scale)),c=document.createElement("canvas");c.width=nw;c.height=nh;c.getContext("2d").drawImage(im,0,0,nw,nh);const b=await new Promise(r=>c.toBlob(r,"image/webp",q));return b?{dataUrl:await blobToDataUrl(b),ow,oh,nw,nh,size:b.size}:null}catch(e){console.warn("CanvasFlow: image compression failed",e);return null}}async function uploadDataImage(o){if(!o||!isDataImage(o.src)||!window.storage||!window.FIREBASE_READY)return false;try{const ext=(o.objectRole==="studyNote"?"png":"webp");const path="whiteboard-images/objects/"+Date.now()+"_"+Math.random().toString(36).slice(2)+"."+ext;const ref=window.storage.ref(path);if(ext==="png"){await ref.putString(o.src,"data_url",{contentType:"image/png"})}else{const r=await compressImage(o.src,1800,.92);if(!r)return false;await ref.putString(r.dataUrl,"data_url",{contentType:"image/webp"})}const url=await ref.getDownloadURL();o.src=url;o.srcUrl=url;return true}catch(e){console.warn("CanvasFlow: Storage image persistence failed",e);return false}}window.canvasflowPersistImageObject=async function(obj){const ok=await uploadDataImage(obj);if(ok){try{if(typeof canvas!=="undefined")canvas.requestRenderAll();if(typeof scheduleSave==="function")scheduleSave(true)}catch(_){} }return ok};async function prepareObject(o){if(!o)return;if(typeof o.srcUrl==="string"&&isHttp(o.srcUrl)){o.src=o.srcUrl;return}if(!isDataImage(o.src))return;const oldW=Number(o.width)||1,oldH=Number(o.height)||1,oldSX=Number(o.scaleX)||1,oldSY=Number(o.scaleY)||1;for(const pair of [[1400,.82],[1200,.72],[1000,.62],[850,.52]]){const r=await compressImage(o.src,pair[0],pair[1]);if(r&&r.size<170*1024){o.src=r.dataUrl;o.width=r.nw;o.height=r.nh;o.scaleX=(oldW*oldSX)/r.nw;o.scaleY=(oldH*oldSY)/r.nh;break}}if(typeof o.srcUrl==="string"&&isDataImage(o.srcUrl))delete o.srcUrl}async function preparePages(pages){if(!Array.isArray(pages))return;for(const page of pages){if(!page||!Array.isArray(page.objects))continue;for(const o of page.objects)await prepareObject(o)}let total=()=>pages.reduce((n,p)=>n+JSON.stringify(p||{}).length,0);if(total()<=TOTAL_BUDGET)return;for(const maxSide of [900,760,620,520]){for(const page of pages){for(const o of (page&&Array.isArray(page.objects)?page.objects:[])){if(!o||!isDataImage(o.src))continue;const r=await compressImage(o.src,maxSide,.28);if(r){const oldW=Number(o.width)||1,oldH=Number(o.height)||1,oldSX=Number(o.scaleX)||1,oldSY=Number(o.scaleY)||1;o.src=r.dataUrl;o.width=r.nw;o.height=r.nh;o.scaleX=(oldW*oldSX)/r.nw;o.scaleY=(oldH*oldSY)/r.nh}}}if(total()<=TOTAL_BUDGET)break}}window.canvasflowPrepareFirestorePages=preparePages;})();</script>`;

const start=s.indexOf('<script id="canvasflow-image-persistence-fix">');
if(start>=0){const end=s.indexOf("</script>",start);if(end>=0)s=s.slice(0,start)+injected+s.slice(end+9)}else{s=s.replace("</body>",injected+"\n</body>")}

const old=`    const currentPageJson = canvas.toJSON(["objectRole","srcUrl","nodeId","arrowHead"]);\n    boardPages[boardPageIndex] = currentPageJson;\n\n    // Serialize before sending to Firestore so a serialization problem is\n    // reported clearly instead of being swallowed as an "unknown" error.\n    const canvasJson = JSON.stringify(currentPageJson);\n    const pagesJson = boardPages.map((pg, i) => {`;
const replacement=`    const currentPageJson = canvas.toJSON(["objectRole","srcUrl","nodeId","arrowHead"]);\n    boardPages[boardPageIndex] = currentPageJson;\n\n    if (typeof window.canvasflowPrepareFirestorePages === "function") {\n      await window.canvasflowPrepareFirestorePages(boardPages);\n    }\n    const canvasJson = JSON.stringify(currentPageJson);\n    const pagesJson = boardPages.map((pg, i) => {`;
if(s.includes(old))s=s.replace(old,replacement);

const oldField=`      canvas: canvasJson,\n      pages: pagesJson,`;
const newField=`      canvas: "",\n      pages: pagesJson,`;
if(s.includes(oldField))s=s.replace(oldField,newField);

fs.writeFileSync(file,s,"utf8");
console.log("CanvasFlow: full-quality image persistence installed.");
